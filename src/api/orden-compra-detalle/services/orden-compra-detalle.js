'use strict';

/**
 * orden-compra-detalle service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::orden-compra-detalle.orden-compra-detalle');
