'use strict';

/**
 * orden-compra-detalle controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::orden-compra-detalle.orden-compra-detalle');
