'use strict';

/**
 * orden-compra-detalle router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::orden-compra-detalle.orden-compra-detalle');
